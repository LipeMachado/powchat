import styled from 'styled-components';

export const BackgroundContainerDiv = styled.div`
    background-color: ${props => props.theme.colors.homePage_Background};
    height: 100vh;
    display: flex;
    overflow: hidden;
`;